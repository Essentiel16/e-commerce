const { productDetailsSchema, ratingValueSchema } = require('./product');
const { signupSchema, loginSchema } = require('./user');

module.exports = {
  productDetailsSchema,
  ratingValueSchema,
  signupSchema,
  loginSchema,
};
